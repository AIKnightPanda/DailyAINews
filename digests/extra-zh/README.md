# 补充源标题译文缓存

Haiku 把补充源条目的英文标题译成中文，一期一个 `<期号>.json`：

```json
{ "1": "中文标题", "2": "中文标题", ... }
```

键是 `extract.js` 给的编号（即 `digests/raw/<期号>.json` 里 `extra.items` 的下标 +1）。

**Haiku 只看得到编号和英文标题，看不到 URL** —— `link-digest.js` 按编号把译文和
URL 配回去。所以模型没有能力编造链接。

缺这个文件或缺某个编号，对应条目退回英文原标题，不影响出稿。
