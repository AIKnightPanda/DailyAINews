# 播客要点缓存

Haiku 子代理通读播客转录全文后产出的中文要点，一期一个 `<期号>.json`：

```json
{ "podcasts": ["第一期的要点…", "第二期的要点…"] }
```

数组顺序对应 `digests/raw/<期号>.json` 里 `podcasts` 的顺序。

**为什么落盘**：`--force` 重写简报时不用重跑 Haiku。
**没有这个文件也能跑**：`extract.js` 会退回定点采样，并在素材里如实标注覆盖率。
